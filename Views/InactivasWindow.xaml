<Window x:Class="FlujoCajaWpf.Views.InactivasWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Flujo de Caja - Propiedades Inactivas" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="{DynamicResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryGradient}" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo y título -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Image Source="/Resources/LogoSamaraRental.PNG" 
                           Width="50" Height="50" 
                           Margin="0,0,15,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Propiedades Inactivas" 
                                   FontSize="20" 
                                   FontWeight="Bold" 
                                   Foreground="White"/>
                        <TextBlock Text="Gestión de propiedades desactivadas" 
                                   FontSize="12" 
                                   Foreground="{StaticResource LightYellowBrush}" 
                                   Opacity="1"/>
                    </StackPanel>
                </StackPanel>

                <!-- Botones de acción -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="✅ Activar Propiedad" 
                            Command="{Binding AbrirDialogoReactivarCommand}"
                            Padding="15,8"
                            Margin="0,0,10,0">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                                <Setter Property="Background" Value="#10B981"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#059669"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    
                    <Button Content="← Volver al Menú" 
                            Click="VolveryMenuButton_Click"
                            Padding="15,8">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                                <Setter Property="Background" Value="#3B82F6"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#2563EB"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Contenido principal -->
        <Grid Grid.Row="1" Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Barra de búsqueda -->
            <Border Grid.Row="0" 
                    Background="{DynamicResource CardBackgroundBrush}" 
                    CornerRadius="8" 
                    Padding="10"
                    Margin="0,0,0,16">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.08" Color="Black"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             Style="{StaticResource ModernTextBox}"
                             Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0"
                             FontSize="15"
                             Padding="4"
                             VerticalAlignment="Center">
                        <TextBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="0"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>

                    <TextBlock Grid.Column="0"
                               Text="🔍 Buscar por nombre de casa, dueño, categoría..."
                               FontSize="15"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="5,0,0,0"
                               IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding TextoBusqueda}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Border>

            <!-- Grid de propiedades -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid MinHeight="570">
                    <ItemsControl ItemsSource="{Binding PropiedadesFiltradas}" VerticalAlignment="Top">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="4"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- Tarjeta de propiedad inactiva -->
                                <Border Background="{DynamicResource CardBackgroundBrush}" 
                                    CornerRadius="12" 
                                    Padding="12"
                                    Margin="5"
                                    MinWidth="200"
                                    MaxWidth="350"
                                    Cursor="Hand"
                                    Opacity="0.85">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="15" 
                                                      ShadowDepth="0" 
                                                      Opacity="0.12" 
                                                      Color="Black"/>
                                </Border.Effect>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect BlurRadius="20" 
                                                                          ShadowDepth="0" 
                                                                          Opacity="0.2" 
                                                                          Color="Black"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Border.InputBindings>
                                    <MouseBinding MouseAction="LeftClick" 
                                                  Command="{Binding DataContext.AbrirCasaCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                  CommandParameter="{Binding}"/>
                                </Border.InputBindings>

                                <StackPanel>
                                    <!-- Nombre -->
                                    <TextBlock Text="{Binding Nombre}" 
                                               FontSize="16" 
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource DarkBlueBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,8"/>
                                    
                                    <!-- Barra acento gris -->
                                    <Border Background="#CBD5E1"
                                            Height="2"
                                            CornerRadius="1"
                                            Margin="0,0,0,8"/>

                                    <!-- Información -->
                                    <TextBlock Text="{Binding CategoriaNombre}" 
                                               FontSize="11"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               Margin="0,0,0,4"
                                               TextTrimming="CharacterEllipsis"/>
                                    
                                    <TextBlock FontSize="11"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               Margin="0,0,0,8">
                                        <Run Text="💰"/>
                                        <Run Text="{Binding Moneda}"/>
                                        <Run Text="  |  👤"/>
                                        <Run Text="{Binding DuenoNombre}"/>
                                    </TextBlock>

                                    <!-- Información de detalle -->
                                    <Border Background="{DynamicResource BackgroundBrush}"
                                            CornerRadius="4"
                                            Padding="8,6">
                                        <TextBlock Text="Clic para ver detalles" 
                                                   FontSize="10"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   TextAlignment="Center"/>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                </Grid>
            </ScrollViewer>

            <!-- Mensaje cuando no hay casas inactivas -->
            <StackPanel Grid.Row="1"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Visibility="{Binding MostrarMensajeVacio, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="✅" 
                           FontSize="72"
                           TextAlignment="Center"
                           Margin="0,0,0,20"/>
                <TextBlock Text="No hay propiedades inactivas" 
                           FontSize="20"
                           FontWeight="Medium"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           TextAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Loading overlay -->
        <Grid Grid.RowSpan="2"
              Background="#80000000"
              Visibility="{Binding EstaCargando, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" 
                             Width="200" 
                             Height="6"
                             Margin="0,0,0,15"/>
                <TextBlock Text="Cargando..." 
                           FontSize="16"
                           Foreground="White"
                           TextAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>

